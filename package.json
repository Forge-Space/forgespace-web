{
  "name": "forgespace-web",
  "version": "0.7.1",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "ads:google:prepublish": "node scripts/google-ads/prepublish.mjs",
    "ads:google:checkpoint": "node scripts/google-ads/checkpoint.mjs",
    "check:cycles": "madge --circular src/"
  }
}